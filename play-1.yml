- name: printing variables
  hosts: web
  vars_prompt:
    - name1: "name1"
      prompt: "enter name1"
  become: yes
  tasks:
    - name: printing variable
      ansible.builtin.debug:
        msg: "hi {{ name1 }}"
