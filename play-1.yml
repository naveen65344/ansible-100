- name: installing ngix server
  hosts: web
  become: yes
  tasks:
   - name: installing nginx server
     ansible.builtin.package:
       name: nginx
       state: present
       enabled: yes

   - name: starting
     ansible.builtin.service:
       name: nginx
       state: started    